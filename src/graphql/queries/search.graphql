query Search($searchTerm: String!) {
  search(query: $searchTerm) {
    id
    name
    album {
      images {
        url
        width
        height
      }
    }
    artists {
      name
    }
    duration_ms
    uri
  }
}
