query UserTopTracks($offset: Int, $limit: Int) {
  userTopTracks(offset: $offset, limit: $limit) {
    id
    name
    album {
      images {
        url
        width
        height
      }
    }
    artists {
      id
      name
    }
    duration_ms
    uri
  }
}
